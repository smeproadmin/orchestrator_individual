import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Server-side rendering enabled — API routes handle AI orchestration
  // Deploy to Vercel, Railway, or any Node.js host with ANTHROPIC_API_KEY env var
};

export default nextConfig;
